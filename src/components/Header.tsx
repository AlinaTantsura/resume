import React from "react";
import {Navigation} from "../components";

const Header = () => {
  return (
    <header className="flex justify-center w-full bg-blue-300 shadow-md">
      <Navigation />
    </header>
  );
};

export default Header;
